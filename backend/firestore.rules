rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // V0.1 receiver records, including FCM tokens, are server-managed only.
    // Firebase Admin SDK requests bypass these rules on the trusted backend.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
