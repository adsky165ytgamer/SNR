# Local Android build setup

Copy `gradle.properties.example` to `gradle.properties` and fill in the original `school-notics` public Firebase client metadata plus the permanent HTTPS backend URL. Do not commit `gradle.properties`, `google-services.json`, keystores, or service-account credentials.
